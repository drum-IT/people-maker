{
  "name": "people-maker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -R build && parcel build index.html -d build --global game --public-url ./ --no-source-maps",
    "dev": "rm -R dev && parcel index.html -d dev --global game --no-source-maps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cssnano": "^4.0.5",
    "parcel-bundler": "^1.9.7"
  },
  "dependencies": {
    "moment": "^2.22.2"
  }
}
